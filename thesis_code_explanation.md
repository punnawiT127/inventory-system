# เอกสารอธิบายการทำงานของระบบหลังบ้าน (MND Store System)
*จัดทำขึ้นเพื่อประกอบการทำปริญญานิพนธ์และตอบคำถามอาจารย์ที่ปรึกษา*

---

## 1. โครงสร้างสถาปัตยกรรม (Architecture)
ระบบนี้พัฒนาขึ้นด้วยรูปแบบ **MVC (Model-View-Controller)** เพื่อแยกส่วนการทำงานให้ชัดเจน:
- **Model (`/models`)**: ใช้ Mongoose เป็นตัวจัดการโครงสร้างข้อมูลที่เชื่อมต่อกับ MongoDB ทำหน้าที่เป็นตัวกลางในการ Insert, Update, Delete ค้นหาข้อมูลใน Database
- **View (`/views`)**: ใช้ EJS (Embedded JavaScript) เป็น Template Engine ในการแสดงผล HTML โดยสามารถฝังตัวแปรจากฝั่ง Server เข้าไปในหน้าเว็บได้ก่อนส่งไปให้ผู้ใช้
- **Controller (`/controllers`)**: เป็นหัวใจหลักในการรับ Request จากหน้าเว็บ นำไปคำนวณ หรือดึงข้อมูลจาก Model แล้วส่งผลลัพธ์ (Response) กลับไปยัง View หรือตอบกลับเป็น JSON
- **Routes (`/routes`)**: ใช้ Express Router ในการกำหนดเส้นทาง URL เช่น ถ้าเข้า URL `/pos` จะให้ไปทำงานที่ฟังก์ชันไหนใน Controller

**Tech Stack ที่ใช้**: Node.js, Express.js, MongoDB (Mongoose), EJS, และใช้ Line Messaging API สำหรับแจ้งเตือน

---

## 2. โครงสร้างฐานข้อมูล (Models)
เบื้องหลังระบบมีการจัดเก็บข้อมูล 4 ส่วนหลัก (Collections):
1. **User.js (ผู้ใช้งาน)**: เก็บข้อมูลพนักงานและเจ้าของร้าน (`username`, `password` ที่ถูกเข้ารหัสด้วย bcrypt, `role` หรือระดับสิทธิ์)
2. **Product.js (สินค้า)**: เก็บข้อมูลสินค้าทั้งหมดในคลัง (`code` บาร์โค้ด, `name`, `price`, `costPrice`, `stock`, `unit`, `weight`)
3. **Sale.js (ประวัติการขาย)**: เก็บข้อมูลบิลแต่ละใบ โดยมีฟิลด์ `items` เก็บ Array ของสินค้าที่ขายไป (จำนวน และราคา ณ ตอนที่ขาย เพื่อป้องกันปัญหาราคาสินค้าเปลี่ยนในอนาคต) และยอดรวม
4. **StockRequest.js (คำร้องขอ)**: เก็บข้อมูลคำร้องขอเพิ่ม/ลดสต็อก กรณีที่พนักงานแก้ไขสต็อก จะต้องรอให้สเตตัส (`status`) ถูกเปลี่ยนจาก 'Pending' เป็น 'Approved' โดยเจ้าของร้านก่อน สต็อกจริงถึงจะเปลี่ยน

---

## 3. อธิบายการทำงานแต่ละหน้า (Line-by-Line / Page-by-Page Logic)

### 3.1 หน้า Dashboard (`views/dashboard.ejs`)
- **หน้าที่**: สรุปยอดขายรวมประจำวัน/เดือน แจ้งเตือนสินค้าขายดี และสินค้าใกล้หมด
- **ข้อมูลที่ส่งไป (Mockup Backend)**: ส่งข้อมูล `filteredTotal`, `monthlyTotal`, `lowStockProducts`, `recentSales` และ `topProducts` เข้ามาแปรผลใน EJS
- **การแก้ไขล่าสุด**: เราได้สลับให้ช่อง **"สินค้าต้องเติมสต็อกด่วน"** ขึ้นมาอยู่เหนือ **"สินค้าขายดี"** เพื่อให้เจ้าของร้านเห็นรายการสินค้าที่หมดสต็อกได้ง่ายและรวดเร็วที่สุด โดยการสลับตำแหน่ง HTML Block ใน EJS

### 3.2 หน้าขายสินค้า POS (`views/pos.ejs` & `saleController.js`)
- **หน้าที่**: หน้าจอสำหรับคิดเงินและจัดการตะกร้าสินค้า
- **การทำงานของโค้ด**:
  - โหลดหน้าเว็บครั้งแรก `saleController.js` จะดึงสินค้าทั้งหมด (`Product.find({ stock: { $gt: 0 } })`) และจัดกลุ่มตามหมวดหมู่ ส่งตัวแปร `products` ไปให้ EJS
  - ในฝั่ง Frontend (Javascript) จะเก็บข้อมูลไว้ในตัวแปร `productsData` และตะกร้าเก็บในตัวแปร `cart`
  - **ช่องค้นหา**: ฟังก์ชัน `searchPOSProducts()` จะจับเหตุการณ์ `onkeyup` เมื่อพิมพ์ชื่อหรือบาร์โค้ด จะใช้ JavaScript วนลูป (forEach) ซ่อนตัวการ์ดสินค้าที่ไม่ตรงกับคำค้นหาทันที (Real-time Filtering)
  - **การปรับจำนวนสินค้า (Quantity)**: เราได้เปลี่ยนจากข้อความธรรมดามาเป็น `<input type="number">` เพื่อให้ตอนขาย พนักงานสามารถพิมพ์ตัวเลข "50" ลงไปได้เลย แทนที่จะต้องกดปุ่มบวก 50 ครั้ง ฟังก์ชัน `updateQty()` จะตรวจสอบว่ายอดที่กรอกใหม่นั้นเกินค่า `maxStock` หรือไม่
  - **การชำระเงิน**: เมื่อกดปุ่ม ระบบจะส่งข้อมูล `cart` ไปที่ `/api/sales` ผ่าน Fetch API ทาง Controller จะทำการตัดสต็อก (ลบ `product.stock`) สร้าง `Sale` document และยิง LINE Notify จากนั้นส่ง `saleId` กลับมา เพื่อให้ Javascript สั่งเปิดหน้าต่างใหม่ `window.open('/receipt/' + data.saleId)` เพื่อพิมพ์ใบเสร็จ

### 3.3 หน้าจัดการสินค้า (`views/products.ejs` & `productController.js`)
- **หน้าที่**: แสดงรายการสินค้าทั้งหมด และสามารถเพิ่ม แก้ไข ลบสินค้าได้
- **แจ้งเตือนบาร์โค้ดซ้ำ (Duplicate Barcode)**:
  - ในฟังก์ชัน `addProduct` จะมีคำสั่ง `const existing = await Product.findOne({ code });`
  - ถ้าระบบพบว่ามีบาร์โค้ดนั้นอยู่แล้ว จะเรียกใช้งาน `lineService.notifyDuplicateBarcode()` เพื่อส่งแจ้งเตือนเข้าไลน์ทันทีว่า "มีความพยายามเพิ่มสินค้าซ้ำ" และ Redirect กลับมาที่หน้าพร้อมพารามิเตอร์ `?error=...` เพื่อแสดงแถบสีแดงแจ้งเตือนหน้าเว็บ
- **ระบบสิทธิพนักงาน (Stock Edit Request)**:
  - ใน `products.ejs` เราดึงค่าระดับสิทธิ์ `const userRole = '<%= user.userRole %>'`;
  - เมื่อกดปุ่ม "บันทึกการเปลี่ยนแปลง" Javascript จะดักจับ Event `submit` ของฟอร์ม
  - ถ้าผู้กดไม่ใช่ Owner และมีการแก้ไขตัวเลขสต็อก โค้ดจะใช้คำสั่ง `e.preventDefault()` เพื่อระงับการบันทึกปกติ และเปลี่ยนไปยิง API เข้า `/api/requests/submit` แทน เพื่อสร้างข้อมูลลงตาราง `StockRequest` และรอเจ้าของร้านมาอนุมัติ

### 3.4 ระบบคำร้องขอหลังบ้าน (`views/requests.ejs` & `requestController.js`)
- **หน้าที่**: เป็นหน้าพิเศษที่เข้าได้เฉพาะเจ้าของร้าน เพื่อดูรายการขอยกเลิกหรือเปลี่ยนสต็อก
- **การทำงาน**:
  - เจ้าของร้านเห็นคำร้อง (Pending) หากกด "อนุมัติ" ฟอร์มจะส่ง Action เข้าสู่ `resolveRequest` 
  - ระบบจะหา `StockRequest` เปลี่ยนสถานะเป็น 'Approved' และนำค่า `request.newStock` ไปบันทึกทับใน `Product.stock` โดยอัตโนมัติ

### 3.5 ใบเสร็จรับเงิน (`views/receipt.ejs`)
- **หน้าที่**: หน้าสำหรับแสดงผลใบเสร็จขนาด 80mm
- **การทำงาน**: รับ `saleId` ผ่าน URL นำไปดึงข้อมูลบิล (พร้อม `populate` ข้อมูลผู้ขายและชื่อสินค้า) มาแสดงผลเป็นตาราง และใช้ `@media print` ใน CSS เพื่อสั่งให้ซ่อนปุ่มเวลาเครื่องพิมพ์ทำงาน

---

## 4. ระบบการส่งข้อมูลแจ้งเตือน (LINE Notify Service)
- อยู่ในไฟล์ `services/lineService.js`
- เป็นศูนย์กลางควบคุมการยิง API เข้าไปที่ Line Channel
- **การแก้ไขล่าสุด**: สำหรับการขายสินค้า ฟังก์ชัน `notifyNewSale(amount, items, sellerName)` ถูกอัปเดตให้รับ Array ของสินค้าที่ขายไป จากนั้นทำการ `.map()` วนลูปเพื่อดึงค่า `productName`, `quantity`, และ `subTotal` มาเรียงต่อกัน (String concatenation) เป็นข้อความแจ้งเตือน ทำให้เจ้าของร้านเห็นใบเสร็จย่อๆ ผ่าน LINE ทุกครั้งที่มีคนกดจ่ายเงินสำเร็จ

## 5. สรุปภาพรวม Data Flow (การไหลของข้อมูลตอนขายสินค้า)
เพื่อให้อาจารย์เห็นภาพรวมที่ชัดเจน นี่คือสเต็ปของข้อมูลตั้งแต่ต้นจนจบเมื่อเกิดการขาย:
1. **[Browser]** พนักงานยิงบาร์โค้ด หรือพิมพ์ค้นหาสินค้า และปรับจำนวนผ่าน `<input>` ในตะกร้า
2. **[Browser]** กดปุ่มชำระเงิน -> ระบบส่ง HTTP POST `JSON` ที่มีรหัสสินค้าและจำนวน ไปยังเส้นทาง `/api/sales`
3. **[Express Route]** `routes/sale.js` ส่งข้อมูลเข้า `saleController.js`
4. **[Controller]** ตรวจสอบว่าในคลังมีของพอไหม -> ลบจำนวนออกจาก `Product.stock` -> บันทึกยอดทั้งหมดลงในตาราง `Sale` ผ่าน Mongoose
5. **[Line SDK]** เรียกฟังก์ชันใน `lineService.js` ส่งข้อความผ่านอินเทอร์เน็ตไปที่ LINE API โดยแนบรายการสินค้าไปด้วย
6. **[Response]** ส่ง `{"success": true, "saleId": "12345"}` กลับไปให้ Browser
7. **[Browser]** โหลดหน้าใบเสร็จ Popup โดยเรียก URL: `/receipt/12345` พร้อมสั่ง `window.location.reload()` เพื่อเคลียร์ตะกร้าและอัปเดตสต็อกหน้าจอ

*(เอกสารนี้ครอบคลุมการอธิบาย Logic และ Mockup Data ครบถ้วนตามมาตรฐานการสอบปริญญานิพนธ์)*
